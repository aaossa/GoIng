// Render new form (with errors)
Rails.$('form')[0].innerHTML = ("<%= j(render partial: 'form', locals: { course: @course }) %>");
// Scroll to top
window.scrollTo({ top: 0, behavior: 'smooth' });
