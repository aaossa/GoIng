// Render new form (with errors)
Rails.$('form')[0].innerHTML = ("<%= j(render partial: 'form', locals: { time_block: @time_block }) %>");
// Scroll to top
window.scrollTo({ top: 0, behavior: 'smooth' });
